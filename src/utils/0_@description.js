// @description tag

/**
 * Add two numbers.
 */
export function add(a, b) {
  return a + b;
}

/**
 * @desc Add two numbers.
 */
export function addTwo(a, b) {
  return a + b;
}

/**
 * @description Add two numbers.
 */
export function addThree(a, b) {
  return a + b;
}
