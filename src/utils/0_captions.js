/** 
 * Some function
 * @param {number} a
 * @param {number} b
 * @param {number} c
 * 
 * @example <caption>Add two numbers</caption>
 * add(2, 4)
 * // returns 6
 * 
 * @example <caption>Add three numbers</caption>
 * add(2, 4, 6)
 * // returns 12
 */
function addThreeNumbers(a, b, c = 0) { 
  return a + b + c 
}